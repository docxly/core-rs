# HWPX Reference Corpus

This directory stores synthetic Hancom-generated reference material used for
HWPX compatibility bring-up.

Rules:

- Do not commit original `.hwpx` binaries here.
- Store only unzipped text entries and provenance notes.
- `paragraph-only/` is the current `Phase A` reference anchor and backs the approved `core-paragraph` fixture.
- `heading-only/` and `linebreak-only/` are placeholders until new synthetic
  Hancom-generated samples are captured.
